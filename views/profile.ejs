<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include ("partials/header") %>
    <title>Login</title>
  </head>
  <body>
    <%- include ("partials/nav") %>
    <h1>Welcome to <%= user.name %>'s page.</h1>
    <img src="<%= user.thumbnail %>" alt="" />
    <p><%= user.googleID %></p>
    <p><%= user.date %></p>

    <% if (posts.length > 0) { %> <% for (let i = 0; i < posts.length; i++) { %>
    <div class="card" style="width: 18rem">
      <div class="card-body">
        <h5 class="card-title"><%= posts[i].make %></h5>
        <p class="card-text"><%= posts[i].type %></p>
        <p class="card-text"><%= posts[i].year %></p>
        <p class="card-text"><%= posts[i].price %></p>
      </div>
    </div>
    <% } %> <% } %>
  </body>
</html>
